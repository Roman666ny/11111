Дементеев Роман УИБО-10-24
Вариант 1. Напишите рекурсивную функцию для вычисления суммы цифр числа

Отлично, разберём **Вариант 1** — рекурсивную функцию для вычисления суммы цифр числа — по всем пунктам.

## 1. Скрипты на Python, C++, Java в файлах репозитория

---

## 2. Описание работы алгоритма

**Аналитика:**
- Число разбивается на последнюю цифру (`n % 10`) и оставшуюся часть (`n // 10`)
- Когда число меньше 10, возвращаем само число
- Последняя цифра + сумма цифр оставшейся части

**Пример для n = 12345:**
```
sum_of_digits(12345)
= 5 + sum_of_digits(1234)
= 5 + 4 + sum_of_digits(123)
= 5 + 4 + 3 + sum_of_digits(12)
= 5 + 4 + 3 + 2 + sum_of_digits(1)
= 5 + 4 + 3 + 2 + 1 = 15
```
---

## 3. Определение временной сложности

**Временная сложность: O(d)**, где **d** — количество цифр в числе.

---

## 4. Почему такая временная сложность

- Каждый рекурсивный вызов обрабатывает одну цифру числа
- Количество вызовов равно количеству цифр в числе
- На каждом уровне выполняется O(1) операций (остаток от деления и целочисленное деление)
- Таким образом, общая сложность **линейно зависит от количества цифр**

**Для числа n количество цифр d = ⌊log₁₀n⌋ + 1**, поэтому можно также сказать, что сложность **O(log n)** по отношению к самому числу.

---

## 5. Ответ на контрольный вопрос

**Вопрос 1:** *Что такое рекурсия и какие два обязательных компонента должна содержать рекурсивная функция?*

**Ответ:** 
Рекурсия — это метод программирования, при котором функция вызывает саму себя для решения задачи. Два обязательных компонента:
1. **Базовый случай** — условие завершения рекурсии, предотвращающее бесконечные вызовы
2. **Рекурсивный случай** — часть функции, где происходит вызов самой себя с изменёнными параметрами, приближающими решение к базовому случаю

**В нашем примере:**
- **Базовый случай:** `if n < 10: return n`
- **Рекурсивный случай:** `return n % 10 + sum_of_digits(n // 10)`

---
